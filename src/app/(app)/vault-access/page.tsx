// Vault Access screen — authentication gate / vault entry
// Demo Screen Builder replaces this with the interactive vault auth UI.
export default function VaultAccessPage() {
  return (
    <div className="p-6 min-h-[400px] flex items-center justify-center">
      <div className="text-center space-y-3">
        <h1 className="text-2xl font-semibold tracking-tight text-foreground">Vault Access</h1>
        <p className="text-sm text-muted-foreground">Authentication gate — Demo Screen Builder builds here.</p>
      </div>
    </div>
  );
}
