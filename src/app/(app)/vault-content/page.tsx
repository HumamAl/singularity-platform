// Vault Content screen — dashboard grid of vault items + session feed
// Demo Screen Builder replaces this with the interactive vault content UI.
export default function VaultContentPage() {
  return (
    <div className="p-6 min-h-[400px] flex items-center justify-center">
      <div className="text-center space-y-3">
        <h1 className="text-2xl font-semibold tracking-tight text-foreground">Vault Content</h1>
        <p className="text-sm text-muted-foreground">Asset grid + session feed — Demo Screen Builder builds here.</p>
      </div>
    </div>
  );
}
